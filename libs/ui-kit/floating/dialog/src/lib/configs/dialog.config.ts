import { DialogConfig } from "../interfaces/dialog-config.interface";

export const DIALOG_CONFIG: DialogConfig = {
  backdropClass: '',
  hasBackdrop: true,
  disableClose: false,
  panelClass: ''
};