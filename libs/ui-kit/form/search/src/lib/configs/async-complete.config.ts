import { AsyncCompleteConfig } from '../interfaces/async-complete-config.interface';

export const asyncCompleteConfig: AsyncCompleteConfig = {
  field: 'name',
  viewValue: 'name',
  debounce: 1000
}