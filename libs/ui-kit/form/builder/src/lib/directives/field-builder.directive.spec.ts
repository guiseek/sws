import { FieldBuilderDirective } from './field-builder.directive';

describe('FieldBuilderDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldBuilderDirective();
    expect(directive).toBeTruthy();
  });
});
